---
interface Props {
  title: string;
  styleClasses?: string[];
}
const { title, styleClasses } = Astro.props;
---

<div class="bg-slate-200 p-5 rounded-md drop-shadow-lg">
  <h3 class="text-2xl m-2">{title}</h3>
  <div class={styleClasses?.join(" ")}>
    <slot />
  </div>
</div>
